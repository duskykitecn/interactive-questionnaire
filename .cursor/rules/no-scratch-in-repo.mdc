---
description: 对公仓库禁止在根目录留下草稿文件；临时产物只放 local/
alwaysApply: true
---

# 草稿与临时文件

本仓库会公开推送。误提交的文件会永远留在 git 历史里。

- 不要在仓库根、技能目录或其它会入库的路径写入问卷 HTML、试写稿、调试输出、点文件草稿。
- 必须落盘时只写 `local/`（该目录除 `.gitignore` 外全部忽略）。文件名不要用点开头，否则资源管理器与 Cursor 会藏起来，用户打不开。
- 不要 `git add local/` 下的内容。
- 优先不落盘：能在回复里说完的不要写成仓库文件。
